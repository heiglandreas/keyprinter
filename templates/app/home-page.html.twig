{% extends '@layout/default.html.twig' %}

{% block title %}Home{% endblock %}

{% block content %}
    <div class="jumbotron">
        <h1>Welcome to the KeyPrinter</h1>
        <p>
            So! You want to go to a key-signing party?
        </p><p>
            Congratulations!
        </p><p>
            This tool can create a sheet with information from your key that
            you can then distribute at the signing-party to other people so they
            can remember your key-information and whether they checkd your ID to
            verify you as the owner of the key.
        </p><p>
            Don't forget to bring an photo-ID with you to the signing party!
        </p>
    </div>

    <div class="row justify-content-center">
        <div class="col-sm-4 align-self-center">
        <form class="form-signin" data-op-form-id="0" method="GET" action="{{ path('key.search') }}">
            <h1 class="h3 mb-3 font-weight-normal">Your KeyData</h1>
            <label for="keyserver" class="sr-only"></label>
            <select name="keyserver" id="keyserver" class="form-control" required="">
                <option value="pool.sks-keyservers.net" selected="selected">Default (pool.sks-keyservers.net)</option>
                <option value="pgp.uni-mainz.de">Uni Mainz</option>
                <option value="gpg.mit.edu">MIT</option>
            </select>
            <label for="keyid" class="sr-only">Searchstring</label>
            <input name="keyid" id="keyid" class="form-control" placeholder="Search for… (email, key-id, name)" required="" type="text" autofocus="">
            <button class="btn btn-lg btn-primary btn-block" type="submit">Fetch Key information</button>
        </form>
        </div>
    </div>
{% endblock %}
